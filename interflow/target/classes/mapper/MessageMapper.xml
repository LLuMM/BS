<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="pw.lumm.dao.MessageMapper">
    <select id="getMessageById" parameterType="String" resultType="pw.lumm.model.Msg">
        select * from message where toid = #{id} order by time desc
    </select>
    <insert id="addMessage" parameterType="pw.lumm.model.Msg">
        insert into message
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="title != null">
                title,
            </if>
            <if test="fromid != null">
                fromid,
            </if>
            <if test="toid != null">
                toid,
            </if>
            <if test="content != null">
                content,
            </if>
            <if test="fromname != null">
                fromname,
            </if>
            <if test="toname != null">
                toname,
            </if>
            <if test="time != null">
                time,
            </if>
            <if test="fid != null">
                fid,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=VARCHAR},
            </if>
            <if test="title != null">
                #{title,jdbcType=VARCHAR},
            </if>
            <if test="fromid != null">
                #{fromid,jdbcType=VARCHAR},
            </if>
            <if test="toid != null">
                #{toid,jdbcType=VARCHAR},
            </if>
            <if test="content != null">
                #{content,jdbcType=VARCHAR},
            </if>
            <if test="fromname != null">
                #{fromname,jdbcType=VARCHAR},
            </if>
            <if test="toname != null">
                #{toname,jdbcType=VARCHAR},
            </if>
            <if test="time != null">
                #{time,jdbcType=TIMESTAMP},
            </if>
            <if test="fid != null">
                #{fid,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>
    <insert id="addNotic" parameterType="pw.lumm.model.Notic">
        insert into notice (id, content, time)
        values (#{id}, #{content}, #{time,jdbcType=TIMESTAMP})
    </insert>
    <select id="getNotices" resultType="pw.lumm.model.Notic">
        select * from notice order by time asc
    </select>

    <delete id="deleteNotice" parameterType="String">
        delete from notice where id = #{id}
    </delete>

    <update id="setMessageStauts" parameterType="String">
        update message set readstatus = 1
        <where>
            <if test="fid != null">
               fid = #{fid}
            </if>
            <if test="id != null">
               id = #{id}
            </if>
        </where>
    </update>

    <delete id="deleteByStatus" parameterType="String">
        delete from message where toid = #{id} where readstatus = 1
    </delete>
    <delete id="deleteMsgById" parameterType="String">
        delete from message where id = #{id}
    </delete>
</mapper>