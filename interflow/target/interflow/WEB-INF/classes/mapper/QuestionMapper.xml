<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="pw.lumm.dao.QuestionMapper" >

    <select id="getQuestionByForunmId" parameterType="String"  resultType="pw.lumm.model.Question">
        select * from question where fid = #{id} and status != 2 order by time desc
    </select>

    <insert id="addQuestion"  parameterType="pw.lumm.model.Question" >
        insert into question (id, userid, fid, title, content, time,uname) values (#{id},#{userid}, #{fid}, #{title},#{content},#{time,jdbcType=TIMESTAMP},#{uname})
    </insert>

    <select id="getQuestionById" parameterType="String"  resultType="pw.lumm.model.Question">
        select * from question where id = #{id} order by time desc
    </select>

    <select id="getAnswerByQid" parameterType="String"  resultType="pw.lumm.model.Answer">
        select * from answer where qid = #{id} order by time asc
    </select>

    <update id="setHitById">
        update question set hits = #{1} where id = #{0}
    </update>

    <update id="setTop">
        update question set stick = #{1} where id = #{0}
    </update>
    <update id="setStatus">
        update question set status = #{1} where id = #{0}
    </update>
    <select id="getQuestionByUserId" parameterType="String" resultType="pw.lumm.model.Question">
        select * from question where UserId = #{uid} order by time desc

    </select>
    <delete id="deleteQuestionById" parameterType="String">
        delete from question where id = #{id}
    </delete>
</mapper>